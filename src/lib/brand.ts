import banner from "@/assets/music-banner.png.asset.json";

/** The MUSIC banner used for the bot welcome message and as brand art. */
export const MUSIC_BANNER_PATH = banner.url;

/** Shared look for every generated image, so all channel posts match the brand. */
export const BRAND_IMAGE_STYLE =
  "grainy dithered gradient background of vivid orange and soft purple with a bright white radial glow, " +
  "clean black and white hand drawn line art subject in the center, flat vector illustration, no text";
